// Backward-compatible re-export. The single source of truth now lives in lib/contacts.ts.
export { contacts, resolveContact, contactName } from "@/lib/contacts";
export type { Contact } from "@/lib/contacts";
